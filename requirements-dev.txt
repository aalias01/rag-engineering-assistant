# Development / evaluation dependencies (not needed for deployment).
# Usage: pip install -r requirements-dev.txt
#
# Version pins: ragas 0.2.x imports langchain_community.chat_models.vertexai,
# which was removed in langchain-community 0.4 — so the langchain stack is
# held at the 0.3 era here. The src/ code is compatible with both eras.
-r requirements.txt
ragas==0.2.15
datasets>=2.19.0
langchain<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-openai>=0.2,<0.3
langchain-text-splitters<0.4
