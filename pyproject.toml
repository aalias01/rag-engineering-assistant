[tool.ruff]
target-version = "py311"
exclude = [
  "notebooks",
]

[tool.ruff.lint.per-file-ignores]
# Inspect AI task files must extend sys.path before importing src/ —
# the E402 late imports there are deliberate.
"evals/*.py" = ["E402"]
