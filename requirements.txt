# Deployment only — Render (pip-based, no conda)
# For local development, use: conda env create -f environment.yml
# Note: sentence-transformers pulls in torch (~700 MB). Render free tier has
# a 512 MB RAM limit — if memory is tight, disable the cross-encoder reranker
# in api/predictor.py and use dense-only retrieval for the deployed version.

fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
langchain-openai==0.2.14
langchain-community==0.3.31
langchain-chroma==0.2.6
langchain-text-splitters==0.3.11
openai==1.109.1
chromadb==1.5.9
rank-bm25==0.2.2
pymupdf==1.27.2.3
python-dotenv==1.2.2
tiktoken==0.13.0
numpy==2.4.6
jsonschema==4.23.0
# ^ v2: validates data/facts/*.json at load (pure Python, tiny)

# v2 note: the LOCAL intent classifier (DistilBERT + LoRA) needs
# torch/transformers/peft and is deliberately NOT listed here -- it's a local
# training option (scripts/train_intent_classifier.py). The deploy default is
# INTENT_CLASSIFIER=zero_shot (Groq free tier) with a rules fallback, which
# needs nothing beyond this file.

# sentence-transformers==5.5.1
#   Left OUT of the default free deploy on purpose. It's only needed for the
#   cross-encoder reranker (USE_RERANKER=true) or local embeddings
#   (EMBEDDING_PROVIDER=local) — both OFF in the free config — and it pulls in
#   torch (~700 MB), which blows the Render free-tier build time and 512 MB RAM.
#   Local dev installs it via conda (environment.yml). Uncomment only if you
#   turn the reranker or local embeddings back on (and expect a slower build).
